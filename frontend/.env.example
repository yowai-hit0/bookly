# Bookly web — copy to .env.local and fill in. Never commit .env files.
# Same origin in every environment: Vite proxies this to Express in development,
# Express serves this build in production. A bare path, never a host.
VITE_API_BASE_URL=/api
